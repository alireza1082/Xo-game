<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Keep the icon background circular and consistent with the splash window. -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/splash_back" />
        </shape>
    </item>
    <!-- The logo art already includes a transparent safe area around its centered mark. -->
    <item android:drawable="@drawable/ic_logo_centered" />
</layer-list>
